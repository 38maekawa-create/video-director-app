# タスク指示書: 29件再バッチ生成

## 目的
FB変換パイプラインの4つの改善（Opus化・切り詰め撤廃・コンテンツライン注入）を全29件に反映し、層判定の誤り（hirai/さるビール/真生）も修正された状態で再生成する。

## 背景
- 2026-03-19に全29件バッチ生成済みだが、当時はmodel=sonnet、3000文字切り詰め、コンテンツライン未注入だった
- 2026-03-20に4修正完了・テスト77件全PASS確認済み
- hirai(層b→層a), さるビール(層b→層a), 真生(層b→層c)の層判定誤りは現コードで修正済み（バッチ再実行で反映される）

## 全体工程における位置づけ
「3つのタイミングで効くナレッジ活用」設計のタイミング①（FB変換の具体化）の改善反映。
これが完了すると、FB変換の品質が全件でOpus+全文品質基準になり、承認フロー設計に進める。

## 実行手順
```bash
cd ~/AI開発10
python3 -m src.video_direction.batch_regenerate --all
```

※ バッチスクリプトのパスが違う場合は以下で確認:
```bash
grep -r "batch" src/ --include="*.py" -l
```

前回の実行ログ: 29件全成功、平均281秒/件、合計約2.3時間

## 完了条件と検証
1. 29件全て成功（エラー0件）
2. hirai=層a, さるビール=層a, 真生=層c で層判定が正しいこと
3. quality_criteriaにセクション5全文（10,698文字）が切り詰められずに注入されていること
4. 結果をPROGRESS.mdに記録
