# タスク: 品質トラッキングダッシュボード実データ連動

<!-- authored: T1/副官A/バティ/2026-03-16 [なおとさん指示: 空きモデルに次タスク投入] -->

## 目的
品質タブのダッシュボードUIを実データと連動させる。現在UIは存在するが、実データが流れていない。

## 背景
- iOS側: QualityDashboardView.swift, DashboardViewModel.swift が存在
- Python側: api_server.py に品質関連エンドポイントが存在
- DBにはプロジェクト29件の実データあり
- 品質スコア（各カテゴリのスコア）はディレクション生成時に計算済み

## やること

### 1. APIエンドポイント確認・追加
- `/api/v1/dashboard/quality` エンドポイントの動作確認
- プロジェクト全体の品質統計（平均スコア、グレード分布、カテゴリ別平均）を返すエンドポイント
- 不足していれば追加

### 2. iOS側のデータ連動
- DashboardViewModel.swift でAPIからデータ取得→UIに反映
- 品質スコアの時系列推移グラフ（簡易版でOK）
- カテゴリ別の平均スコア表示
- 改善傾向の可視化

### 3. テスト追加
- APIエンドポイントのテスト
- テスト全PASS確認

## 完了条件
- 品質ダッシュボードに実データが表示される
- テスト全PASS
- PROGRESS.md更新

## 絶対ルール
- 既存コードの削除禁止
- AppThemeに従う
